package Until

import StoreEngine.value.{IntValue, Value}

/**
  * Created by jianwei.yang on 2017/5/11.
  */
class CommonUntil {


/*       def mergeList[T](dataTypeList:List[String])(valueList:List[String]):List[T]={
         if(dataTypeList.length==valueList.length){
           val iterator1=dataTypeList.toIterator
           val iterator2=valueList.toIterator
           while(iterator1.hasNext){
            val value= Value.getMatchValue(iterator1.next())
             value.write(iterator2.next())
           }
         }


       }*/

/*  def getValue[T](value:Value): T ={
         value match {
           case IntValue=>value.asInstanceOf[IntValue].get()
     }
  }*/
}
